[project]
name = "insta-reach"
version = "0.1.0"
description = "Collect Instagram comments through an existing Chrome CDP session"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "playwright==1.55.0",
]

[project.scripts]
insta-reach = "insta_reach:main"
